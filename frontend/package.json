{
  "dependencies": {
    "@babel/preset-react": "^7.22.3",
    "babel-loader": "^9.1.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "webpack": "^5.84.1",
    "webpack-cli": "^5.1.1"
  },
  "scripts": {
    "build": "npx webpack-cli build --mode production",
    "develop": "npx webpack-cli build --mode development --watch",
    "start": "npm install && npm run build && start http://127.0.0.1:80/"
  }
}
